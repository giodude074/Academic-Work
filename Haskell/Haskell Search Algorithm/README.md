### Specifications:

```
Implement a search algorithm that searches through a list for Int n and returns the value in the list after n. 

Implement a search algorithm that searches through a list for Int n and returns the value in the list before n.

Implement a function that calculates a digit sum of an Int --- adds all the digits  of the int together, returning an Int.
```