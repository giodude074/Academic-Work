### Specifications:

```
Replace the error handling in the previous exercise by having the method throw an ArrayIndexOutOfBoundsException, printing "You can only enter 5 numbers."

Add the try...catch blocks to your main method: do this when you call your addPosInteger method. 
```
